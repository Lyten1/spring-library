databaseChangeLog:
  - changeSet:
      id: add-user-isDeleted
      author: lyten
      changes:
        - addColumn:
              tableName: users
              columns:
                - column:
                    name: is_deleted
                    type: bit(1)
                    defaultValueBoolean: false
                    constraints:
                      nullable: false
